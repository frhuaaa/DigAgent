# Legacy unsafe examples

`result_old/` contains historical files preserved only for provenance. They use
obsolete paths and behavior, including test metrics in ordinary training logs,
an ICIR checkpoint example, and pre-contract experiment identifiers.

Do not read this directory during implementation, formal execution, Agent
context construction, testing, or Memory Bank retrieval. It is not an API,
schema, runnable result, or source of current DiagAgent behavior.
