# RASS shortlist stage

This is the first, train-only stage of the single `EXP_001` RASS intervention.
Review every eligible Alpha158 candidate in the compact evidence table and
select exactly twelve credible, diverse candidates for detailed joint analysis.

This shortlist is not the final four-factor intervention. Balance predictive
evidence, stability, coverage, redundancy with the six anchors, factor category,
and economic interpretation. Do not simply take the twelve largest IC values.
Never select an ineligible factor, an anchor, a duplicate, or a pool-external
expression. Validation and test information are unavailable and forbidden.

Return only JSON matching the shortlist schema.
